<settings>
    <!-- omitted xml -->
    <servers>
        <server>
            <id>internal</id>
            <username>${env.ARCHIVA_USERNAME}</username>
            <password>${env.ARCHIVA_PASSWORD}</password>
        </server>
        <server>
            <id>snapshots</id>
            <username>${env.ARCHIVA_USERNAME}</username>
            <password>${env.ARCHIVA_PASSWORD}</password>
        </server>
        <!-- omitted xml -->
    </servers>
    <!-- omitted xml -->
</settings>